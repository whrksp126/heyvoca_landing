---
// 지금의 헤이보카 — 정량 지표 4종.
// dictWordCount, dictExampleCount는 src/data/dictionaryStats.ts에서 import.
import { dictWordCount, dictExampleCount, bookstoreBookCount, cumulativeUsers } from '../data/dictionaryStats';
import CountUpNumber from './demo/CountUpNumber.tsx';

const stats = [
  { value: dictWordCount, label: '사전 단어 개수' },
  { value: bookstoreBookCount, label: '서점 단어장 개수' },
  { value: dictExampleCount, label: '예문 (자체 사전)' },
  { value: cumulativeUsers, label: '누적 서비스 사용자' },
];
---

<section class="section bg-surface hairline-t hairline-b" aria-labelledby="proof-h2">
  <div class="container-content">
    <div class="max-w-3xl" data-reveal>
      <span class="eyebrow">
        <span class="h-1.5 w-1.5 rounded-full bg-mute"></span>
        지금의 헤이보카
      </span>
      <h2
        id="proof-h2"
        class="mt-6 text-balance text-[32px] font-bold leading-[1.12] tracking-tight text-ink md:text-display-md lg:text-display-lg"
      >
        과장 없이, 정직하게.
      </h2>
      <p class="mt-5 max-w-prose text-body-lg text-sub text-pretty">
        지금까지 만든 것들과, 함께 만들어 갈 것들 — 솔직하게 적어둡니다.
      </p>
    </div>

    <dl
      class="mt-12 grid gap-x-8 gap-y-10 md:grid-cols-2 lg:grid-cols-4"
      data-reveal-stagger
      data-stagger-ms="100"
    >
      {stats.map((s) => (
        <div>
          <dt class="text-caption font-medium uppercase tracking-[0.12em] text-mute">{s.label}</dt>
          <dd class="mt-3 text-balance text-[40px] font-bold leading-none tracking-tight text-ink md:text-display-md lg:text-display-lg">
            <CountUpNumber value={s.value} client:visible />
          </dd>
        </div>
      ))}
    </dl>
  </div>
</section>
